import React from 'react';
import ReactDOM from 'react-dom';
// import './index.css';

// import axios from 'axios';
import App from './App';

// 默认配置
// axios.defaults.baseURL = "https://httpbin.org" 
// axios.defaults.headers.common["token"] = "adada"

ReactDOM.render(
  <App />,
  document.getElementById('root')
);

